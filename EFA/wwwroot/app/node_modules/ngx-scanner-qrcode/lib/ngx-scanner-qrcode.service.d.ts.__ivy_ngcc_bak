import { AsyncSubject } from 'rxjs';
import { ScannerQRCodeConfig, ScannerQRCodeSelectedFiles } from './ngx-scanner-qrcode.options';
export declare class NgxScannerQrcodeService {
    /**
     * Load files
     * @param files
     * @return AsyncSubject
     */
    loadFiles(files?: File[]): AsyncSubject<ScannerQRCodeSelectedFiles[]>;
    /**
     * loadFilesToScan
     * @param files
     * @param config
     * @returns
     */
    loadFilesToScan(files: File[], config: ScannerQRCodeConfig): AsyncSubject<ScannerQRCodeSelectedFiles[]>;
    /**
     * readAsDataURL
     * @param file
     * @return Promise
     */
    private readAsDataURL;
}
